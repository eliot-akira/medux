{
  "name": "medux",
  "version": "1.1.1",
  "description": "Medux: Modular immutable state management",
  "main": "index.js",
  "scripts": {
    "dev": "babel src --out-dir build --extensions \".js,.ts,.tsx\" --watch & sleep 3 && nodemon --watch build build/test",
    "build": "babel src --out-dir build --extensions \".js,.ts,.tsx\"",
    "test": "yarn build && node build/test",
    "type": "tsc",
    "clean": "rm -rf build",
    "release": "yarn build && yarn types && cp package.json build && cp readme.md build && cd build && npm publish"
  },
  "dependencies": {
    "immer": "5.x"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/preset-env": "^7.8.3",
    "@babel/preset-react": "^7.8.3",
    "@types/react": "^16.9.17",
    "babel-plugin-add-module-exports": "^1.0.2",
    "concurrently": "^5.0.2",
    "nodemon": "^2.0.2",
    "testra": "^1.0.9",
    "typescript": "^3.7.5"
  },
  "license": "MIT",
  "author": "Eliot Akira",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eliot-akira/medux.git"
  }
}
