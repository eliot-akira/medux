{
  "name": "medux",
  "version": "0.1.9",
  "description": "Medux: Modular immutable state management",
  "main": "index.js",
  "scripts": {
    "dev": "babel src --out-dir build --watch & sleep 3 && nodemon --watch build build/test",
    "build": "babel src --out-dir build",
    "test": "node build/test",
    "release": "cp package.json build && cp readme.md build && cd build && npm publish"
  },
  "dependencies": {
    "immer": "^5.3.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/preset-env": "^7.8.3",
    "@babel/preset-react": "^7.8.3",
    "concurrently": "^5.0.2",
    "nodemon": "^2.0.2"
  },
  "license": "MIT",
  "author": "Eliot Akira",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eliot-akira/medux.git"
  }
}
